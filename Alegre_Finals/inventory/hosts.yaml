all:
  vars:
    # Default SSH user; can be overridden per-host
    ansible_user: isaac
    # ansible_ssh_private_key_file: "~/.ssh/id_rsa"

  children:
    centos:
      hosts:
        centos_host:
          ansible_host: 192.168.56.118
          ansible_user: jhonisaacalegre

    # Playbook groups
    db_servers:
      hosts:
        centos_host: {}

    monitoring_server:
      hosts:
        centos_host: {}

    monitored_nodes:
      children:
        db_servers: {}
        monitoring_server: {}
